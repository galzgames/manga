# 🔥 Configuração Firebase - Gestão de Mangás

## 📋 Pré-requisitos

- Uma conta Google (Gmail)
- O projeto está pronto para ser integrado ao Firebase

---

## 🚀 Passo a Passo de Configuração

### **PASSO 1: Criar um Projeto no Firebase**

1. Acesse [Firebase Console](https://console.firebase.google.com/)
2. Clique em **"Adicionar projeto"**
3. Preencha o nome: `gestao-de-mangas` (ou o nome que preferir)
4. Desative **"Analytics"** (você pode ativar depois se quiser)
5. Clique em **"Criar projeto"**
6. Aguarde a criação (leva alguns segundos)
7. Clique em **"Continuar"**

---

### **PASSO 2: Copiar as Credenciais**

1. Na página do projeto, clique no ícone ⚙️ (Engrenagem) no lado esquerdo
2. Selecione **"Configurações do projeto"**
3. Desça até a seção **"Seus apps"**
4. Procure por um aplicativo web (ícone `</>`). Se não houver, clique em **"Adicionar app"** e selecione **Web**
5. Copie o bloco de código `firebaseConfig` (aquele que começa com `const firebaseConfig = {`)

---

### **PASSO 3: Colar as Credenciais**

1. Abra o arquivo **`firebase-config.js`** na pasta do projeto
2. Substitua os valores placeholders pelas credenciais copiadas:

```javascript
const firebaseConfig = {
    apiKey: "AIzaSyD...",
    authDomain: "seu-projeto.firebaseapp.com",
    projectId: "seu-projeto-id",
    storageBucket: "seu-projeto.appspot.com",
    messagingSenderId: "123456789",
    appId: "1:123456789:web:abcdef123456"
};
```

3. **Salve o arquivo**

---

### **PASSO 4: Configurar Firestore Database**

1. No Firebase Console, vá para **"Firestore Database"** (no menu esquerdo)
2. Clique em **"Criar banco de dados"**
3. Selecione a região: **"South America (São Paulo - southamerica-east1)"** (ou próximo de você)
4. Modo de segurança: Selecione **"Iniciar no modo teste"**
5. Clique em **"Criar"**

> ⚠️ **Nota importante:** O modo teste permite leitura/escrita sem autenticação. Você deve mudar para modo produção depois, configurando regras de segurança.

---

### **PASSO 5: Configurar Regras de Firestore (Segurança)**

1. No Firestore, vá para a aba **"Regras"**
2. Substitua o conteúdo pelas regras abaixo (permite que qualquer pessoa leia e escreva):

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura e escrita para todos (modo teste)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

3. Clique em **"Publicar"**

> 🔒 **Para produção:** Configure autenticação e regras mais restritivas

---

### **PASSO 6: Configurar Firebase Storage**

1. No Firebase Console, vá para **"Storage"**
2. Clique em **"Criar bucket"**
3. Nome: `seu-projeto.appspot.com` (sugerido automaticamente)
4. Selecione a região: **"South America (São Paulo)"**
5. Selecione **"Iniciar no modo teste"**
6. Clique em **"Criar"**

---

### **PASSO 7: Configurar Regras de Storage**

1. No Storage, vá para **"Regras"**
2. Substitua pelas regras abaixo:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permite ler e escrever em qualquer arquivo
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
```

3. Clique em **"Publicar"**

---

## ✅ Verificando a Configuração

1. Abra o site no navegador
2. Abra o **Console do Navegador** (F12 → Aba "Console")
3. Procure pela mensagem: **"✅ Firebase iniciado com sucesso!"**

Se aparecer a mensagem, tudo está configurado corretamente! 🎉

---

## 📱 Como Usar Agora

- **Adicionar mangá:** Os dados serão salvos no Firebase Firestore
- **Upload de capa:** As imagens irão para Firebase Storage
- **Histórico de estoque:** Salvo no Firestore
- **Funciona offline:** Dados são sincronizados quando voltar online

---

## 🔐 Segurança em Produção

**IMPORTANTE:** As configurações acima estão em "modo teste" (qualquer pessoa pode ler/escrever). Para produção:

1. Configure **Firebase Authentication** (adicione login)
2. Atualize as regras de Firestore e Storage para exigir autenticação
3. Configure variáveis de ambiente para as credenciais

Exemplo de regra segura:
```javascript
match /{document=**} {
  allow read, write: if request.auth != null;
}
```

---

## 🐛 Solução de Problemas

### "Firebase não está definido"
- Verifique se os scripts do Firebase estão carregando (F12 → Network)
- Confirme que `firebase-config.js` está no mesmo diretório

### "Erro ao fazer upload de imagem"
- Verifique as regras de Storage
- Confirme que o Storage foi criado

### "Dados não aparecem"
- Abra F12 e veja os erros no Console
- Confirme que Firestore Database foi criado

---

## 📚 Recursos Úteis

- [Documentação Firebase](https://firebase.google.com/docs)
- [Console Firebase](https://console.firebase.google.com/)
- [Guia Firestore](https://firebase.google.com/docs/firestore)
- [Guia Storage](https://firebase.google.com/docs/storage)

---

**Precisa de ajuda?** Verifique o Console do navegador (F12) para mensagens de erro detalhadas.
